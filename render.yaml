services:
  - type: web
    name: journey-map
    runtime: docker
    region: singapore
    plan: starter
    disk:
      name: submissions-data
      mountPath: /app/submissions
      sizeGB: 1
    envVars:
      - key: ADMIN_KEY
        generateValue: true
      - key: NODE_ENV
        value: production
